---
name: NetworkPowerMonitor
tagline: Android measurement app that couples network state and battery draw through adaptive sampling, so transition-time power cost is actually observable.
description: Android app that couples network state and battery draw into a single timeline. Adaptive sampling bursts at transition boundaries, so WiFi-to-cellular handoffs get millisecond-resolution data while idle periods stay cheap on battery and storage.
tags: ['Android', 'Kotlin', 'Networking']
codeHref: https://github.com/hdprajwal/NetworkPowerMonitor
hasContent: false
---
